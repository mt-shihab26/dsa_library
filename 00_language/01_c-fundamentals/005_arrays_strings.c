#include <stdio.h>

int main() {
    int nums[3] = {10, 20, 30};
    printf("value of index 1: %d\n", nums[1]);

    char name[] = "Alice";
    printf("value of char array is: %s\n", name);

    return 0;
}
