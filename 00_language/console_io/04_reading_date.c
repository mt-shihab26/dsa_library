#include <stdio.h>

int main() {
    int day, month, year;
    scanf("%d/%d/%d", &month, &day, &year);

    printf("%d %d %d", month, day, year);

    /*
    Helps when input is of format

    01/29/64
    */
}