#include <bits/stdc++.h>
using namespace std;


int main(void) {
    int t;
    cin >> t;
    string line;
    cin.ignore();
    while (t--) {
        getline(cin, line);
        cout << line << endl;
    }

    return 0;
}
