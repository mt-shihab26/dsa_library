#include <bits/stdc++.h>
using namespace std;

int main(void) {
    string str;
    int x, y;
    char op, eq;
    string va;
    
    cin >> str;
    stringstream is(str);
    is >> x >> op >> y >> eq >> va;
}