#include <bits/stdc++.h>
using namespace std;

class Queue
{
public:
    Queue()
    {

    }
}

int
main()
{
    Queue q;
    q.push(100);
    q.push(200);
    cout << q.front() << ed;
    cout << q.front() << ed;
    q.pop();
    cout << q.front() << ed;
    q.pop();
    cout << q.front() << ed;
    q.push(100);
    q.push(200);
    cout << q.front() << ed;
    q.pop();
    cout << q.front() << ed;
}